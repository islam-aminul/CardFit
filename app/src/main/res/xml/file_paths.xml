<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Share copies live in cacheDir/shared and are exposed only via FileProvider grants. -->
    <cache-path name="shared" path="shared/" />
</paths>
