import { cn } from '../lib/cn'

// Small outlined tag/pill used for attributes (e.g. "Fully offline").
export default function Tag({ children, className = '' }) {
  return (
    <span
      className={cn(
        'inline-flex items-center rounded-full border border-midnight-100 px-3 py-1 text-xs text-midnight-600',
        className,
      )}
    >
      {children}
    </span>
  )
}
