import VoiceArcsMark from './VoiceArcsMark'

// Service offering card. The icon tile reuses the voice-arc mark — the
// signal motif, restated small.
export default function ServiceCard({ title, body }) {
  return (
    <div className="group h-full rounded-2xl border border-midnight-100 bg-white p-6 transition-colors hover:border-midnight-200">
      <span className="inline-flex h-11 w-11 items-center justify-center rounded-xl bg-midnight-50 transition-colors group-hover:bg-teal-500/10">
        <VoiceArcsMark className="h-6 w-6" />
      </span>
      <h3 className="mt-5 text-lg font-semibold text-midnight-800">{title}</h3>
      <p className="mt-3 text-sm leading-relaxed text-midnight-600/90">{body}</p>
    </div>
  )
}
