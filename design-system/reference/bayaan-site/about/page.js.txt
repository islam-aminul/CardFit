import Container from '../components/Container'
import Button from '../components/Button'
import Eyebrow from '../components/Eyebrow'
import Reveal from '../components/Reveal'
import ArcField from '../components/ArcField'
import { cn } from '../lib/cn'

export const metadata = {
  title: 'About',
  description:
    'Bayaan Consultancy is a product studio and software consultancy built on clarity and privacy by design.',
}

const principles = [
  {
    title: 'Clarity',
    tone: 'teal',
    body: 'The name says it. We write clear code, give clear advice, and build interfaces people understand on the first try.',
  },
  {
    title: 'Privacy by design',
    tone: 'sage',
    body: 'We collect as little as possible — often nothing at all — and we treat that as the standard, not the exception.',
  },
  {
    title: 'Ownership',
    tone: 'sage',
    body: 'We build our own products, so we feel the full weight of every decision — from the first commit to the support inbox.',
  },
  {
    title: 'Honesty',
    tone: 'teal',
    body: 'We tell you what we would do, what we would not, and why. No upselling, no jargon, no surprises.',
  },
]

const bars = { teal: 'bg-teal-500', sage: 'bg-sage-500' }

export default function AboutPage() {
  return (
    <>
      <section className="relative overflow-hidden border-b border-midnight-100 bg-white">
        <ArcField tone="light" cx={960} cy={250} />
        <Container className="relative py-20">
          <Eyebrow>About</Eyebrow>
          <h1 className="mt-4 max-w-3xl text-4xl font-semibold text-midnight-800 sm:text-5xl">
            Software, built with clarity.
          </h1>
          <p className="mt-6 max-w-prose text-lg leading-relaxed text-midnight-600/90">
            <span className="font-medium text-midnight-800">Bayaan</span> (بیان) means clear, eloquent expression — to
            state something plainly and well. That idea runs through everything we make.
          </p>
          <p className="mt-4 max-w-prose leading-relaxed text-midnight-600/90">
            Bayaan Consultancy is a product studio and software consultancy based in India. We design, build, and ship
            web and mobile software — both our own apps and products for the people we work with. We are bringing our
            first products to market now, built the way we like to build: focused, offline-first where it counts, and
            respectful of the people who use them.
          </p>
        </Container>
      </section>

      <section>
        <Container className="py-16">
          <Eyebrow tone="sage">What we stand for</Eyebrow>
          <h2 className="mt-3 text-3xl font-semibold text-midnight-800">Four principles, held to.</h2>
          <div className="mt-10 grid gap-8 sm:grid-cols-2">
            {principles.map((p, i) => (
              <Reveal key={p.title} delay={(i % 2) * 0.06}>
                <div className="h-full rounded-2xl border border-midnight-100 bg-white p-8">
                  <div className={cn('h-1 w-10 rounded-full', bars[p.tone])} />
                  <h3 className="mt-4 text-xl font-semibold text-midnight-800">{p.title}</h3>
                  <p className="mt-3 leading-relaxed text-midnight-600/90">{p.body}</p>
                </div>
              </Reveal>
            ))}
          </div>

          <div className="relative mt-12 overflow-hidden rounded-3xl border border-midnight-100 bg-paper p-10 sm:p-14">
            <h2 className="text-2xl font-semibold text-midnight-800">Let&apos;s build something clear.</h2>
            <p className="mt-3 max-w-md text-midnight-600/90">
              Whether it is a new product or a project that needs rescuing, we would like to hear about it.
            </p>
            <div className="mt-7">
              <Button href="/contact">Get in touch</Button>
            </div>
          </div>
        </Container>
      </section>
    </>
  )
}
