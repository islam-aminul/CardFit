/** The canonical Bayaan brand mark: concentric voice arcs radiating from a dot. */
export interface VoiceArcsMarkProps {
  /** Rendered square size in px. Default 36. */
  size?: number;
  /** White-on-dark variant for midnight surfaces. Default false. */
  light?: boolean;
  className?: string;
}
