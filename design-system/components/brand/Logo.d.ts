/** Full Bayaan logo lockup: voice-arcs mark + "Bayaan / CONSULTANCY" wordmark. */
export interface LogoProps {
  /** White wordmark for midnight surfaces. Default false. */
  light?: boolean;
  className?: string;
}
