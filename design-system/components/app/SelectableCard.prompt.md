Text-only select chip for single/multi picks (e.g. card-size override: Automatic / Force CR-80 / Custom).

```jsx
<SelectableCard label="Automatic" selected onClick={…} />
<SelectableCard label="Custom" onClick={…} />
```
