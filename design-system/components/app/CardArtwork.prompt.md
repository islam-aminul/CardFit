Stylized card-type illustration for the card-type grid. Geometry matches the Compose original; accents remapped to Bayaan (pan → midnight-600, aadhaar → teal-500, epic → midnight-700, admit → teal-600, custom → midnight-300, free → sage-500).

```jsx
<CardArtwork type="aadhaar" />
```

Always 1.585:1 (CR-80). Never add real document logos/seals.
